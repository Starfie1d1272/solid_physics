# Homework 4: 火星内核射线追踪（TauP）

> **Due Date:** 2026/03/30

## 📝 任务概述
本次作业使用 ObsPy 的 TauP 工具，复现并解释火星内核结构对地震射线路径与震相走时的影响。核心工作是构建“无固态内核 / 有固态内核”两套 1D 模型，在统一参数下做对比分析。

## 本次完成情况（两题）
- 题目一：已完成（模型构建、射线路径图、27° 两表对比、文字分析）。
- 题目二：本次未完成（时间不足），后续补充。

题目二预留：
- 目标与方法：`[后续补充]`
- 图表与结果：`[后续补充]`
- 结论：`[后续补充]`

## 具体要求
1. 构建两套火星 1D 速度模型并编译为 TauP 模型。
2. 在震源深度 33 km、震中距 27° 下绘制射线路径图（对应 Fig1a/Fig1b 对比思路）。
3. 分析受固态内核影响的震相，并比较走时差异。
4. 给出结果解释，说明与 Nature 文献图的异同。

## 当前实现（本目录）
- 主分析文件：`hw4.ipynb`
- 核心输出图：`figures/mars_ray_paths_final.png`
- 模型文件目录：`models/`

## 当前结果摘要（27°）
- 无核模型可见分支（作业定义）：`PKKP`, `P'P'n`, `P'P'r_ab`, `P'P'r_bc`
- 有核模型可见分支（作业定义）：`PKiKP`, `PKIIKP`, `PKKP`, `P'P'n`, `P'P'r_ab`, `P'P'r_df`
- 共有分支：`PKKP`, `P'P'n`, `P'P'r_ab`
- 仅无核出现：`P'P'r_bc`
- 仅有核出现：`PKiKP`, `PKIIKP`, `P'P'r_df`

## 📁 文件结构
- `hw4.ipynb`：作业主 Notebook（模型构建、射线绘图、震相走时对比）。
- `models/`：`.nd` 与 `.npz` 模型文件。
- `figures/`：图件输出目录。
- `HW4_Report.md`：作业报告框架（已提供，可继续补充结果细节）。

## 运行步骤
1. 安装依赖（如缺包）：
   - `pip install obspy matplotlib pandas numpy`
2. 打开并运行 `hw4.ipynb`。
3. 建议按单元顺序执行：
   - 单元 1：环境与参数设置
   - 单元 2：模型构建与编译
   - 单元 3：27° 射线路径图
   - 单元 4：27° 震相走时两表对比（最终展示）

## 输出结果（提交建议）
1. 图件：`figures/mars_ray_paths_final.png`
2. 表格：Notebook 中“无核/有核模型可见震相表”两张。
3. 报告：完善 `HW4_Report.md` 中占位内容后导出提交。

## 调试记录（精简）
1. `.nd` 模型需严格标注 `outer-core` / `inner-core`，否则 TauP 可能无法正确解析核边界。
2. 复杂深部相需要在相位池中显式加入高阶相名，否则默认搜索可能缺失。
3. 射线绘图采用笛卡尔坐标转换而非直接极坐标插值，以避免轨迹畸变。
4. 为对齐文献版式，对优弧分支做镜像展示（仅影响可视化，不影响物理结果）。
5. `PKPPKP` 使用两阶段分类（先行程后深度）以稳定区分 `n/ab/bc`。

## 参考文献
- Nature 文章链接：
  - https://www.nature.com/articles/s41586-025-09361-9
